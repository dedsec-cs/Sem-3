## Unit 2 Questions ⚙️

1.  Use the **Round Robin scheduling algorithm** to schedule five processes and compute the turnaround time.
2.  Difference between **Multilevel Queue** and **Multilevel Feedback Queue Scheduling**.
3.  Explain **Round Robin Scheduling** with an example.
4.  Describe the various states in a **Process Control Block (PCB)**.
5.  Explain the concept of **starvation** in scheduling and methods to handle it.
6.  Compare and contrast **SJF** and **Round Robin scheduling** with examples.
7.  Explain the structure of a **Linux process management system**.
8.  Discuss the different types of **process scheduling queues**.
9.  Consider the following set of processes with their respective arrival times (AT) and burst times (BT), scheduled using the **First-Come, First-Served (FCFS) algorithm**:
    Calculate:
    1.  The completion time (CT) for each process.
    2.  The average waiting time (AWT).
    3.  The average turnaround time (TAT).
    Show your calculations.
10. Consider the following set of processes with their respective arrival times (AT) and burst times (BT). The scheduling follows the **First-Come, First-Served (FCFS) algorithm**. Tasks:
    1.  Construct a **Gantt Chart** for this scheduling.
    2.  Calculate the **Completion Time (CT)** for each process.
    3.  Determine the **Turnaround Time (TAT)** and **Waiting Time (WT)** for each process.
    4.  Compute the **Average Waiting Time (AWT)** and **Average Turnaround Time (ATAT)**.
    5.  Interpret the results: How does the arrival time of P3 (which is earlier than P4) affect the overall waiting time of processes?
11. Using **FCFS**, calculate for each process:
    * Completion Time (CT)
    * Turnaround Time $(\text{TAT} = \text{CT} - \text{AT})$
    * Waiting Time $(\text{WT} = \text{TAT} - \text{BT})$
    Also calculate:
    * ATAT (Average Turnaround Time)
    * AWT (Average Waiting Time)
    * ACT (Average Completion Time)
12. Use **SJF (non-preemptive)** to calculate for each process:
    * Completion Time (CT)
    * Turnaround Time $(\text{TAT} = \text{CT} - \text{AT})$
    * Waiting Time $(\text{WT} = \text{TAT} - \text{BT})$
    Also calculate:
    * ATAT (Average Turnaround Time)
    * AWT (Average Waiting Time)
    * ACT (Average Completion Time)

---

13. Time Quantum: 3 milliseconds. Draw the **Gantt Chart**, then compute:
    * Completion Time (CT)
    * Turnaround Time (TAT)
    * Waiting Time (WT)
    * Averages (ATAT, AWT, ACT)
14. Use **Preemptive SJF (SRTF)** to calculate CT, TAT, WT, ATAT, AWT, ACT.
15. **Note: Lower number = higher priority**. Task: Schedule the processes using **non-preemptive priority scheduling**. Calculate CT, TAT, WT, ATAT, AWT, ACT.
16. Time Quantum: 2 milliseconds. Task: Draw the **Gantt Chart** and compute CT, TAT, WT, ATAT, AWT, ACT.
17. Consider the set of 5 processes whose arrival time and burst time are given below - If the CPU scheduling policy is **priority non-preemptive**, calculate the average waiting time and average turnaround time. (Higher number represents higher priority)
18. Solve for the average waiting time and turnaround time using the **First Come First Serve (FCFS) algorithm** for the following processes:
19. Compute the average turnaround time using the **Shortest Job First (SJF) non-preemptive algorithm** for these processes:
20. Analyze the response time and waiting time using **Shortest Remaining Time First (SRTF)** for the given dataset:
21. Derive the execution order and CPU utilization using **preemptive priority scheduling** where lower numbers indicate higher priority:
22. Describe the **Process Transition Diagram** with a labeled diagram.
